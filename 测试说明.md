# 测试说明

## 1. 目录说明

当前工作目录：

```powershell
F:\Desk\graduation project
```

自动化测试项目目录：

```powershell
F:\Desk\graduation project\ecommerce-autotest
```

被测电商系统目录：

```powershell
F:\Desk\shop
```

## 2. 测试目标

本项目用于对本地电商后台系统进行自动化测试，当前联调目标如下：

- 后端 API：`http://localhost:5000`
- 登录 API：`http://localhost:5000/api/login`
- 前端登录页：`http://localhost:8080/login.html`
- 前端首页：`http://localhost:8080/index.html`

## 3. 环境准备

建议先确认本机具备以下环境：

- Python 3.9+
- Chrome 浏览器
- 已安装 `ecommerce-autotest/requirements.txt` 中依赖

安装依赖命令：

```powershell
cd "F:\Desk\graduation project\ecommerce-autotest"
pip install -r requirements.txt
```

## 4. 启动被测系统

### 4.1 启动后端

进入电商系统后端目录并启动 Flask 服务：

```powershell
cd "F:\Desk\shop\backend"
python run.py
```

默认后端地址：

```text
http://localhost:5000
```

### 4.2 启动前端

进入前端目录并启动一个本地静态服务器：

```powershell
cd "F:\Desk\shop\frontend"
python -m http.server 8080
```

启动后访问：

```text
http://localhost:8080/login.html
```

### 4.3 默认测试账号

管理员账号：

```text
用户名：admin
密码：admin123
```

普通用户账号：

```text
用户名：user1
密码：user123
```

## 5. 配置文件说明

当前自动化项目已默认对接本地前端登录页：

文件：

- [config.yaml](/f:/Desk/graduation%20project/ecommerce-autotest/config/config.yaml)
- [login.yaml](/f:/Desk/graduation%20project/ecommerce-autotest/config/testdata/login.yaml)

当前关键配置：

- `environment.base_url = http://localhost:8080`
- 登录测试数据已按本地系统账号调整

如果更换被测系统地址，需要同步修改这两个文件。

## 6. 如何执行测试

**前提说明**：以下命令默认在项目根目录（`F:\Desk\graduation project`）下执行，除非另有说明。

### 6.1 快速开始（推荐新手）

如果你第一次运行测试，建议按以下顺序执行：

1. **启动被测系统**（如果尚未启动）：
   ```powershell
   # 启动后端
   cd "F:\Desk\shop\backend"
   python run.py
   ```
   ```powershell
   # 启动前端（新开终端）
   cd "F:\Desk\shop\frontend"
   python -m http.server 8080
   ```

2. **环境检查**：
   ```powershell
   cd "F:\Desk\graduation project\ecommerce-autotest"
   python run.py --check
   ```

3. **运行登录模块测试**（验证基础功能）：
   ```powershell
   cd "F:\Desk\graduation project"
   pytest ecommerce-autotest/testcases/test_login.py -vv
   ```

4. **运行全部测试**（如果登录测试通过）：
   ```powershell
   cd "F:\Desk\graduation project"
   pytest
   ```

### 6.2 运行全部测试

执行项目中的所有测试用例：

```powershell
pytest
```

**说明**：
- 这会收集 `ecommerce-autotest/testcases` 目录下的所有测试文件。
- 当前项目已经修复了最初的导入错误、包路径错误、浏览器工厂缺失问题。
- 但并不代表所有业务测试都已适配当前本地电商系统，部分模块仍可能失败。

### 6.3 按模块运行测试

如果你只想测试特定功能模块：

**登录模块**：
```powershell
pytest ecommerce-autotest/testcases/test_login.py -vv
```

**运行登录模块中的单条测试**：
```powershell
pytest ecommerce-autotest/testcases/test_login.py::TestLogin::test_valid_login -vv
```

**商品模块**：
```powershell
pytest ecommerce-autotest/testcases/test_product.py -vv
```

**订单模块**：
```powershell
pytest ecommerce-autotest/testcases/test_order.py -vv
```

**用户模块**：
```powershell
pytest ecommerce-autotest/testcases/test_user.py -vv
```

**权限模块**：
```powershell
pytest ecommerce-autotest/testcases/test_permission.py -vv
```

### 6.4 按标记运行测试

当前已在根目录 `pytest.ini` 中注册了一些标记，可以通过标记筛选测试：

**运行冒烟测试**：
```powershell
pytest -m smoke -vv
```

**运行登录相关测试**：
```powershell
pytest -m login -vv
```

**运行商品相关测试**：
```powershell
pytest -m product -vv
```

### 6.5 使用项目主脚本执行

项目提供了 `run.py` 脚本，更适合按项目封装方式执行：

**检查环境**：
```powershell
cd "F:\Desk\graduation project\ecommerce-autotest"
python run.py --check
```

**查看可用测试模块**：
```powershell
python run.py --list
```

**运行特定模块**：
```powershell
python run.py --run login
```

**运行全部测试**：
```powershell
python run.py --run all
```

**说明**：
- `run.py` 更适合按项目封装方式执行，会自动处理环境检查和路径设置。
- `pytest` 更适合日常调试和定位问题，灵活性更高。

### 6.6 高级选项

#### 6.6.1 无头模式（后台运行浏览器）

如果你希望浏览器在后台运行（不显示界面），需要修改配置文件：

1. 打开 `ecommerce-autotest/config/config.yaml`
2. 将 `browser.headless` 设置为 `true`：
   ```yaml
   browser:
     headless: true
   ```

之后运行任何测试都会在无头模式下执行。

#### 6.6.2 生成测试报告

使用 `pytest-html` 插件生成HTML测试报告：

```powershell
pytest --html=report.html --self-contained-html
```

报告将生成在项目根目录下的 `report.html` 文件中。

#### 6.6.3 调试技巧

- **查看详细输出**：添加 `-v` 或 `-vv` 参数获取更详细的测试输出
- **只收集不执行**：使用 `--collect-only` 查看有哪些测试用例
- **失败时暂停**：使用 `--pdb` 在测试失败时进入Python调试器
- **失败重试**：使用 `pytest-rerunfailures` 插件自动重试失败的测试

## 7. 目前已修复的测试框架问题

以下问题已经修复：

- 根目录运行 `pytest` 时无法导入 `utils`
- 根目录运行 `pytest` 时无法导入 `testcases`
- 缺少 `utils.browser_factory`
- 缺少 `driver` fixture
- `LoginPage` 的 `url` 被父类初始化覆盖的问题

相关文件：

- [conftest.py](/f:/Desk/graduation%20project/conftest.py)
- [pytest.ini](/f:/Desk/graduation%20project/pytest.ini)
- [browser_factory.py](/f:/Desk/graduation%20project/ecommerce-autotest/utils/browser_factory.py)

## 8. 当前已知限制

当前项目处于“框架可运行，但业务适配未完全收口”的状态。

已确认：

- 登录成功链路可对接本地系统
- 登录失败链路能捕获错误信息
- 浏览器驱动可正常创建

但以下模块仍可能失败：

- 商品管理
- 订单管理
- 用户管理
- 权限管理

原因通常是：

- 页面定位器仍是旧系统结构
- 断言逻辑与本地系统实际页面不一致
- 数据前置条件不足


## 9. 测试失败时优先检查

如果测试失败，优先检查以下几点：

### 9.1 服务是否启动

- `http://localhost:5000` 是否可访问
- `http://localhost:8080/login.html` 是否可访问

### 9.2 账号密码是否正确

当前对接的是：

- `admin / admin123`
- `user1 / user123`

### 9.3 浏览器是否可用

当前浏览器工厂优先使用本机 Chrome。

### 9.4 是否是页面定位器失效

如果报错是 `element not found`、`timeout`、`clickable` 之类，通常是页面对象和当前系统页面结构不匹配。

## 10. 相关关键文件

- [conftest.py](/f:/Desk/graduation%20project/conftest.py)
- [pytest.ini](/f:/Desk/graduation%20project/pytest.ini)
- [run.py](/f:/Desk/graduation%20project/ecommerce-autotest/run.py)
- [base_test.py](/f:/Desk/graduation%20project/ecommerce-autotest/testcases/base_test.py)
- [login_page.py](/f:/Desk/graduation%20project/ecommerce-autotest/pages/login_page.py)
- [browser_factory.py](/f:/Desk/graduation%20project/ecommerce-autotest/utils/browser_factory.py)
- [config.yaml](/f:/Desk/graduation%20project/ecommerce-autotest/config/config.yaml)
- [login.yaml](/f:/Desk/graduation%20project/ecommerce-autotest/config/testdata/login.yaml)

## 11. 当前结论

这套自动化测试项目现在已经可以：

- 在根目录直接执行 `pytest`
- 创建浏览器驱动
- 对接本地电商系统登录页
- 执行登录相关自动化测试

后续如果要继续提升通过率，建议优先做：

1. 把登录模块彻底跑绿
2. 逐步适配商品、订单、用户、权限模块页面对象
3. 再执行完整回归测试
