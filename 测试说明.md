# 测试说明

## 1. 目录说明

当前工作目录：

```powershell
F:\Desk\graduation project
```

自动化测试项目目录：

```powershell
F:\Desk\graduation project\ecommerce-autotest
```

被测项目目录：

```powershell
F:\Desk\shop-system
```

## 2. 测试目标

本项目用于对本地电商后台系统 `shop-system` 进行自动化测试。

当前联调目标如下：

- 前端地址：`http://localhost:3000`
- 后端 API：`http://localhost:8083`
- Swagger 地址：`http://localhost:8083/swagger-ui/index.html`

## 3. 环境准备

建议先确认本机具备以下环境：

- Python 3.9+
- Chrome 浏览器
- Node.js / npm
- JDK 17+（或项目要求的 Java 版本）
- Maven

安装自动化测试依赖：

```powershell
cd "F:\Desk\graduation project\ecommerce-autotest"
pip install -r requirements.txt
```

## 4. 启动被测系统

### 4.1 启动后端

进入 `shop-system` 后端目录：

```powershell
cd "F:\Desk\shop-system\backend"
```

启动 Spring Boot：

```powershell
mvn spring-boot:run -DskipTests
```

启动后默认访问：

```text
http://localhost:8083
```

### 4.2 启动前端

进入 `shop-system` 前端目录：

```powershell
cd "F:\Desk\shop-system\frontend"
```

首次运行先安装依赖：

```powershell
npm install
```

启动前端开发服务器：

```powershell
npm run dev
```

启动后默认访问：

```text
http://localhost:3000
```

## 5. 配置文件说明

自动化测试当前应对接 `shop-system`：

- [config.yaml](/f:/Desk/graduation%20project/ecommerce-autotest/config/config.yaml)
- [login.yaml](/f:/Desk/graduation%20project/ecommerce-autotest/config/testdata/login.yaml)

当前关键配置应保持与 `shop-system` 一致：

- `environment.base_url = http://localhost:3000`
- `environment.api_base_url = http://localhost:8083/api`

如果后续端口或环境变化，需要同步修改以上配置。

## 6. 如何执行测试

以下命令默认在项目根目录执行：

```powershell
cd "F:\Desk\graduation project"
```

### 6.1 快速开始

1. 启动被测系统后端：

```powershell
cd "F:\Desk\shop-system\backend"
mvn spring-boot:run -DskipTests
```

2. 启动被测系统前端：

```powershell
cd "F:\Desk\shop-system\frontend"
npm run dev
```

3. 环境检查：

```powershell
cd "F:\Desk\graduation project\ecommerce-autotest"
python run.py --check
```

4. 运行登录模块测试：

```powershell
cd "F:\Desk\graduation project"
pytest ecommerce-autotest/testcases/test_login.py -vv
```

5. 再运行其他模块：

```powershell
pytest ecommerce-autotest/testcases/test_product.py -vv
pytest ecommerce-autotest/testcases/test_order.py -vv
pytest ecommerce-autotest/testcases/test_user.py -vv
pytest ecommerce-autotest/testcases/test_permission.py -vv
```

### 6.2 运行全部测试

```powershell
pytest
```

### 6.3 按模块运行测试

登录模块：

```powershell
pytest ecommerce-autotest/testcases/test_login.py -vv
```

商品模块：

```powershell
pytest ecommerce-autotest/testcases/test_product.py -vv
```

订单模块：

```powershell
pytest ecommerce-autotest/testcases/test_order.py -vv
```

用户模块：

```powershell
pytest ecommerce-autotest/testcases/test_user.py -vv
```

权限模块：

```powershell
pytest ecommerce-autotest/testcases/test_permission.py -vv
```

### 6.4 按标记运行测试

```powershell
pytest -m smoke -vv
pytest -m login -vv
pytest -m product -vv
pytest -m order -vv
pytest -m user -vv
pytest -m permission -vv
```

### 6.5 使用项目脚本执行

```powershell
cd "F:\Desk\graduation project\ecommerce-autotest"
python run.py --check
python run.py --list
python run.py --run login
python run.py --run all
```

## 7. 测试报告

生成 HTML 报告：

```powershell
pytest --html=report.html --self-contained-html
```

## 8. 当前已知情况

当前自动化项目已经具备基础可运行能力，但仍处于“框架可运行、业务页面持续适配中”的状态。

已确认：

- 登录页已切换到 `shop-system`
- `pytest` 可在根目录直接运行
- 浏览器驱动和公共夹具已接通

仍需持续适配：

- 商品模块
- 订单模块
- 用户模块
- 权限模块

常见失败原因：

- 页面定位器仍是旧版本结构
- 页面路由或入口和被测项目实际实现不一致
- 测试数据前置条件不足
- 被测系统服务未启动或端口不可访问

## 9. 测试失败时优先检查

### 9.1 服务是否启动

- `http://localhost:3000` 是否可访问
- `http://localhost:8083/actuator/health` 是否可访问

### 9.2 测试配置是否正确

检查：

- [config.yaml](/f:/Desk/graduation%20project/ecommerce-autotest/config/config.yaml)
- [login.yaml](/f:/Desk/graduation%20project/ecommerce-autotest/config/testdata/login.yaml)

### 9.3 浏览器是否可用

当前默认使用 Chrome。

### 9.4 是否为页面定位器失效

如果报错为以下类型，通常优先怀疑测试项目适配问题：

- `element not found`
- `TimeoutException`
- `element click intercepted`
- `not clickable`

## 10. 相关关键文件

- [conftest.py](/f:/Desk/graduation%20project/conftest.py)
- [pytest.ini](/f:/Desk/graduation%20project/pytest.ini)
- [run.py](/f:/Desk/graduation%20project/ecommerce-autotest/run.py)
- [base_test.py](/f:/Desk/graduation%20project/ecommerce-autotest/testcases/base_test.py)
- [login_page.py](/f:/Desk/graduation%20project/ecommerce-autotest/pages/login_page.py)
- [config.yaml](/f:/Desk/graduation%20project/ecommerce-autotest/config/config.yaml)
- [login.yaml](/f:/Desk/graduation%20project/ecommerce-autotest/config/testdata/login.yaml)

## 11. 当前结论

这套自动化测试现在对接的被测项目是：

```text
F:\Desk\shop-system
```

不是：

```text
F:\Desk\shop
```

后续所有测试说明、启动方式、端口和页面适配，都应以 `shop-system` 为准。
